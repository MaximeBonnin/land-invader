info:
  name: POST Score
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/api/v1/score
  body:
    type: json
    data: |-
      {
        "score": 3,
        "name": "test2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
