info:
  name: GET Score
  type: http
  seq: 1

http:
  method: GET
  url: localhost:8080/api/v1/score
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
